# Screenshots

Add your own screenshots here. The README references:

- `dashboard.png` — the executive Dashboard sheet (the one with the KPI cards, the two charts, and the slicer).

Suggested extras (optional, strengthen the repo for a technical reviewer):

- `analisis_transito.png` — the exception engine sheet showing the traffic-light conditional formatting on the flag columns.
- `power_query.png` — the Power Query editor showing the applied steps of `Transit_Enriched`.
- `fair_share.png` — the Fair_Share sheet showing the shortage detection and proportional allocation.

Tip: crop screenshots tightly and keep them readable. Name the main one exactly `dashboard.png` so the README image link works without edits.
